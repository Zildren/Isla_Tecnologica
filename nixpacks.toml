[phases.setup]
nixPkgs = ["nodejs_20", "jdk17_headless"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = [
  "cd frontend && npm run build",
  "mvn -f backend/pom.xml clean package -DskipTests"
]

[start]
cmd = "java -jar backend/target/demo-0.0.1-SNAPSHOT.jar"